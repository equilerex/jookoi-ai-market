# Copy to .env (gitignored). Read by `npm run sync`.
# Comma-separated folders that receive a copy of every skill in plugins/*/skills/.
# Add your own extras. Default if .env is missing: the two below.
SKILL_TARGETS=~/.agents/skills,~/.claude/skills