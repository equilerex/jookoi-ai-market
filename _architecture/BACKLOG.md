# Backlog — logged, not yet scoped
<!-- Deliberately unordered. Pull an item into TODO.md's checklist when it gets a real slot. See AGENTS.md. -->

## Angular skill: test budgets on lazy chunks

Status: OPEN

Does anyScript or a bundle budget fire on lazy chunks in Angular 22? Largest lazy chunk in the reference run was under its cap, so untested. One temporary build with a low anyScript cap answers it. Then update the unverified lists in chunk-size.md and build-and-deploy.md.
